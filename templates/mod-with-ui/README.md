# mod-with-ui

This folder contains optional reference snippets for a future mod settings UI.

Notes:

- these examples are not compiled into the current build
- they are for a later phase, after the external host and bridge contract are defined
- do not treat them as active implementation for the current project milestone
